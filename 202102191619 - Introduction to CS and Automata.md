You need an alphabet for an automata
def Alphabet. Any finite set of symbols $\sum$ and $\sum$* for 
def String. A string $\omega$ over an alphabet $\sum$ is a finite set fo simbols from that alphabet.
